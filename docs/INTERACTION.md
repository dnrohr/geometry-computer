# Interaction model

The diagram, generated step list, object inspector, proof card, and expression tree share semantic object and step IDs. Hover is transient; click selection persists. Operation badges state operation, inputs, output, geometric method, and proof availability. Scaffolding visibility changes rendering only and never recompiles the expression.
